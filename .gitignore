.env
.gh-token
node_modules/
.DS_Store

# 清理回收筒（Stephanie 全局規則：清理一律 mv 去 _to_delete/，唔好推上 GitHub）
_to_delete/

# session-lock.sh 嘅鎖檔（Drive mount rm 唔到會改名做 .DELETE-ME-*，兩種都唔好推上 GitHub）
.active-session.lock*

# 改舊檔前嘅備份（S3 規則產物，唔推上 GitHub — 同 lock 檔/_to_delete 同類）
*.bak-*

# Google Drive Mirror 衝突副本（兩機並行時 Drive 會自動整多份 "xxx (1).ext"）
# 一律唔准推上 GitHub（2026-08-16 加，10 個副本曾差啲夾帶）
* (1)*
* (2)*
* (3)*
*conflicted copy*
